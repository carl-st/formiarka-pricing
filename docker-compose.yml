services:
  api:
    build: .
    image: stl-pricing:latest
    environment:
      - PORT=3000
      - RATES_PATH=/app/config/rates.json
    volumes:
      - ./config/rates.json:/app/config/rates.json:ro
      - ./data/tmp:/app/tmp
    ports:
      - "3000:3000"
